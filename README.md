# Big_Data_Project
## Project Overview — what the pipeline does and why it exists
## Architecture — ASCII diagrams for the batch, streaming, and CI/CD layers
## Tech Stack — clean table covering every tool from PostgreSQL to Hive
## Repository Structure — folder layout with file-level descriptions
## Pipeline Modes — explains full load, incremental, and streaming
## Jenkins CI/CD — step-by-step flow + why Jenkins is used
## Pipeline Parameters — the 4 parameters (LOAD_TOOL, LOAD_TYPE, LOAD_SCOPE, TABLE_NAME) in a table
## How to Run — Jenkins steps + manual bash commands as fallback
## Branch Strategy — including the current feature/add-jenkinsfile branch

To use it, just copy the contents into your README.md file on the feature/add-jenkinsfile branch and adjust any folder names or script names if they differ from what's shown.
